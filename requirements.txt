# SLCA-GRPO top-level Python dependencies.
#
# Policy: minimum pinning - fix a lower bound where the API surface matters,
# leave upper bounds open so users can track upstream.
#
# Tested stack (what the paper ran on):
#   python 3.10,  torch 2.3.1+cu121,  vllm 0.8.5,
#   transformers 4.47.1,  ray 2.42.1,  evalscope 0.17.0
#
# verl itself is installed from the vendored tree:
#   cd verl && pip install -e . --no-deps

# ----- Core deep-learning stack -----
torch>=2.3,<2.8
transformers>=4.47,<4.56
accelerate>=1.0.0
peft>=0.14
sentencepiece>=0.2
tokenizers>=0.20
safetensors>=0.4
einops>=0.7

# ----- Inference / serving -----
vllm>=0.8,<0.12
flash-attn>=2.6 ; platform_machine == "x86_64"

# ----- Distributed RL -----
ray[default]>=2.10,<2.50
datasets>=2.19
pyarrow>=15.0
pandas>=2.0
numpy>=1.26,<2.3

# ----- Config / orchestration -----
hydra-core>=1.3
omegaconf>=2.3
aiohttp>=3.9
requests>=2.31
tenacity>=8.2
loguru>=0.7

# ----- Reward / judge plumbing -----
openai>=1.30

# ----- Evaluation (BFCL-v3 / tau2-Bench) -----
evalscope>=0.17
modelscope>=1.18

# ----- Experiment tracking -----
wandb>=0.17
tensorboard>=2.15

# ----- Dev / tests -----
pytest>=8.0
